{
  "image": "golioth/golioth-ncs-base:v2.5.0-SDK-v0",
  "onCreateCommand": {
    "list files": "mkdir app && mv west.yml app/ && west init -l app"
  },
  "customizations": {
    "vscode": {
      "extensions": ["nordic-semiconductor.nrf-connect-extension-pack"],
      "settings": {
        "terminal.integrated.defaultProfile.linux": "nRF Connect"
      }
    }
  }
}